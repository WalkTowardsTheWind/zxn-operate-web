export enum contractStatus {
  "正常",
  "过期",
  "停用",
}
export enum contractType {
  "业务拓展协议(个人)" = 1,
  "业务拓展协议(企业)" = 2,
  "共享经济服务协议" = 3,
  "自由职业者服务协议" = 4,
}
export enum contractTerm {
  "一年" = 1,
  "二年" = 2,
  "三年" = 3,
}
export enum onlineType {
  "线上",
  "线下",
}
